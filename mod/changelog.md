# v0.2.0
- Fixed authorization on newer Discord builds: when the in-app popup can't
  grant voice permissions, authorization completes in the browser instead
- Connection status now shows the actual failure reason

# v0.1.0
- Initial release
